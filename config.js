// Replace these values with your actual Tebex credentials and configuration
export const tebexSecret = 'YOUR_TEBEX_SECRET_KEY'; // Your Tebex secret key
export const tebexBaseUrl = 'https://plugin.tebex.io'; // Tebex API base URL

// Configuration for intervals
export const config = {
    checkIntervalTicks: 20, // Interval in ticks (20 ticks = 1 second)
    apiTimeoutSeconds: 30   // API timeout in seconds
};